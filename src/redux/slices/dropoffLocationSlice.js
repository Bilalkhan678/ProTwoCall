// dropoffLocationSlice.js

// import { createSlice } from '@reduxjs/toolkit';

// const initialState = {
//   dropoffLocation: null,
// };

// const dropoffLocationSlice = createSlice({
//   name: 'dropoffLocation',
//   initialState,
//   reducers: {
//     setDropoffLocation(state, action) {
//       state.dropoffLocation = action.payload;
//       localStorage.setItem('dropoffLocation', JSON.stringify(action.payload));
//     },
//     clearDropoffLocation(state) {
//       state.dropoffLocation = null;
//       localStorage.removeItem('dropoffLocation');
//     },
//   },
// });

// export const { setDropoffLocation, clearDropoffLocation } = dropoffLocationSlice.actions;

// export default dropoffLocationSlice.reducer;

// import { createSlice } from "@reduxjs/toolkit";

// const initialState = {
//   dropoffLocation: null,
// };

// const dropoffLocationSlice = createSlice({
//   name: "dropoffLocation",
//   initialState,
//   reducers: {
//     setDropoffLocation(state, action) {
//       state.dropoffLocation = action.payload;
//       localStorage.setItem("dropoffLocation", JSON.stringify(state.dropoffLocation));
//     },
//     clearDropoffLocation(state) {
//       state.dropoffLocation = null;
//       localStorage.removeItem("dropoffLocation");
//     },
//   },
// });

// export const { setDropoffLocation, clearDropoffLocation } = dropoffLocationSlice.actions;

// export default dropoffLocationSlice.reducer;


